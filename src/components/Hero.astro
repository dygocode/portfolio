<section id="hero" class="hero-section flex items-center bg-[#272621] text-areia px-6 py-16 md:py-20 overflow-hidden relative">
  <div class="hero-layout max-w-6xl w-full mx-auto grid grid-cols-1 gap-12 lg:gap-16 items-center">
    <div class="text-center lg:text-left relative z-10">
      <h1 class="text-4xl md:text-6xl font-bold mb-6 font-space-grotesk leading-tight">
        Transformamos ideias em
        <span class="text-ambar">soluções digitais</span> sob medida
      </h1>

      <p class="text-lg md:text-xl text-areia/80 mb-10 max-w-2xl mx-auto lg:mx-0 font-helvetica leading-relaxed">
        Somos especialistas em descobrir exatamente o que seu negócio precisa e propor
        soluções que geram resultados reais. Na DYGO, sua confiança é nosso maior ativo.
      </p>

      <div class="flex flex-col sm:flex-row gap-4 justify-center lg:justify-start">
        <a href="#contato" class="hero-action inline-flex min-h-11 items-center justify-center bg-ambar text-grafite font-semibold px-8 py-4 rounded-lg hover:bg-mel focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-mel transition-colors duration-300 font-space-grotesk">
          Fale Conosco
        </a>
        <a href="#processo" class="hero-action inline-flex min-h-11 items-center justify-center border-2 border-ambar text-ambar font-semibold px-8 py-4 rounded-lg hover:bg-ambar hover:text-grafite focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-mel transition-colors duration-300 font-space-grotesk">
          Conheça Nosso Processo
        </a>
      </div>
    </div>

    <figure class="hero-media w-full max-w-lg mx-auto relative z-10">
      <img
        src="/hero-1-1024.webp"
        srcset="/hero-1-544.webp 544w, /hero-1-1024.webp 1024w"
        sizes="(max-width: 639px) 272px, (max-width: 1023px) 512px, 40vw"
        alt="Rei de xadrez em tom âmbar, representando estratégia digital"
        width="1024"
        height="1280"
        loading="eager"
        decoding="async"
        fetchpriority="high"
        class="hero-image"
      />
    </figure>
  </div>
</section>

<style>
  .hero-section {
    min-height: calc(100svh - 4rem);
  }

  .hero-image {
    display: block;
    width: 100%;
    height: auto;
    aspect-ratio: 4 / 5;
    object-fit: cover;
    object-position: center;
    border-radius: 1.25rem;
  }

  @media (min-width: 768px) {
    .hero-section {
      min-height: calc(100svh - 5rem);
    }
  }

  @media (min-width: 1024px) {
    .hero-layout {
      grid-template-columns: minmax(0, 1.12fr) minmax(340px, 0.88fr);
    }
  }

  @media (max-width: 639px) {
    .hero-media {
      max-width: 17rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-action {
      transition-duration: 0.01ms;
    }
  }
</style>
